// Notación

// ; --> Delimita el final de una linea

const b = 4

b + 4 ;


// . --> Se utiliza en los objetos para acceder a los atributos
// movil.anchura

// [] --> Se utiliza para listas, arreglos o arrays
const ar = [1, 2, 3, 4, 5]
console.log(ar[2])

// () --> Funciones, métodos, operaciones
function suma(a, b) {
  //Se escribe la función
}

// {} --> Objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}
console.log(movil.anchura)

if (true) {
  //Se escribe la estructura de control
  const constante2 = "hola"
  console.log(constante2);

}
